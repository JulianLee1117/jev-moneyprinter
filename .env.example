# Local environment takes precedence over this ignored .env file.
# Only these named credentials are read; no shell interpolation or execution occurs.
OPENROUTER_API_KEY=
SCRY_API_KEY=
# Historical market data only. No order placement is implemented.
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
